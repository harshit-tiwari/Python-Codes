Program to input a list and display its reverse in one line
we will have top siplay it as a list

import sys


n = int(input().strip())
arr = [int(arr_temp) for arr_temp in input().strip().split(' ')]
arrNew = str()
data = 0
for i in range(0,n):
    arrNew = arrNew + str(arr[n-i-1])+ ' '
    
    
print(arrNew)    
    
    
