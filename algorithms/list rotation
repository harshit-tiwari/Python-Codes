Input a list
Rotate the list k number of times
then display the elements of the list

input - 
->length of list, number of rotations, number of elements to be displayed from the rotated list
->list


import sys


n,k,q = input().strip().split(' ')
n,k,q = [int(n),int(k),int(q)]
a = [int(a_temp) for a_temp in input().strip().split(' ')]
for a0 in range(q):
    m = int(input().strip())

    
for j in range(k):
    temp2 = a[n-1]
    for i in range(n):
        temp1 = a[i]
        a[i] = temp2
        temp1, temp2 = temp2, temp1

for i in range(q):
    print(a[i])
